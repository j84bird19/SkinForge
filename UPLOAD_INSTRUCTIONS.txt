SKINFORGE — CLEAN RESPONSIVE RESET

Upload and replace BOTH:
- flow-builder.css
- body-builder.js

This is not another CSS patch. It completely replaces the accumulated flow-builder stylesheet.

Fixes:
- Removes the conflicting stacked overrides that broke the UI.
- Keeps the A/B/C menus vertical at every screen width.
- Prevents browser-default white buttons.
- Keeps the full body above the bottom view cards.
- Hides the old mobile Tools / Canvas / Layers bar while Body Builder is open.
- Uses the same desktop-style structure even when Chrome reports a narrow phone viewport.
- Restores the regular SkinForge panels and navigation when Body Builder closes.

After committing, fully close the browser tab and reopen SkinForge so the old CSS is not reused.
